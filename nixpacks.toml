# Force Python provider (Railway auto-detects Node.js due to frontend/package.json)
providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn bundle_analyzer.api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
